import { StrategyWorkspace } from "@/components/dashboard/strategy-workspace";

export default function TeamStrategySetupPage() {
  return <StrategyWorkspace initialScope="teams" initialSection="setup" />;
}
